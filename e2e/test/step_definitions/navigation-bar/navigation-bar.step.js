
const {When, Then, And} = require 'cucumber'

  When(/^User navigates to current portfolio webpage$/, function (callback) {
    callback.pending();
  });

  Then(/^A new browser window opens$/, function (callback) {
    callback.pending();
  });

  And(/^Navigates to navigation bar$/, function (callback) {
    callback.pending();
  });

  And(/^Clicks on resume link$/, function (callback) {
    callback.pending();
  });

  And(/^displays the resume.pdf file$/, function (callback) {
    callback.pending();
  });

